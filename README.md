# Middleware-Microsoft-Graph-API
Middleware para centralizar a comunicação das automações (RPA) com a Microsoft Graph API.  Abstrai a complexidade de autenticação OAuth2, gerencia renovação de tokens e contextos de segurança, permitindo que robôs e scripts interajam com SharePoint e Exchange (E-mail) utilizando apenas uma chave de API interna e chamadas REST simples.
